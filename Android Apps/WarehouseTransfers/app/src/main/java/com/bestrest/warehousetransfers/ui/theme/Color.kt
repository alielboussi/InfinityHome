package com.bestrest.warehousetransfers.ui.theme

import androidx.compose.ui.graphics.Color

// Matches Infinity Home web dashboard theme (global-theme.css)

/** Dark page background — --dash-bg */
val IhBg = Color(0xFF0A0A08)

/** Card / panel surface — --dash-surface */
val IhSurface = Color(0xFF111613)

/** Raised surface — --dash-surface-2 */
val IhSurfaceAlt = Color(0xFF151D18)

/** Primary green — --dash-border / action green */
val IhGreen = Color(0xFF1ED7A8)

/** Stronger green for filled primary buttons */
val IhGreenStrong = Color(0xFF2A9D8F)

/** Accent blue — --dash-accent */
val IhBlue = Color(0xFF63C7FF)

/** Secondary blue used on web (#00b4d8) */
val IhBlueStrong = Color(0xFF00B4D8)

/** Main text — --dash-text */
val IhText = Color(0xFFF1F5F2)

/** Muted text — --dash-muted */
val IhMuted = Color(0xFFA5B4AD)

/** Error / danger red (web delete / error) */
val IhRed = Color(0xFFE53935)

/** Soft error text */
val IhRedSoft = Color(0xFFFF6B6B)

val IhWhite = Color(0xFFFFFFFF)
val IhOnGreen = Color(0xFF05110E)
